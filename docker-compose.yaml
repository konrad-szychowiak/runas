version: "3.8"

services:

  db:
    container_name: "runas-db-postgres"
    hostname: "runasdb"
    image: "postgres"
    ports:
      - "5432:5432"
    env_file:
      - .env

  server:
    container_name: "runas-server"
    build:
      context: ./server
      dockerfile: Dockerfile
    ports:
      - "8080:8080"